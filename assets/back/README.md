# assets/back

**Back** (player-side) sprite sheets. Same naming as `assets/front`.

Sources: the pack's `Graphics/Pokemon/Back/` folder.

To fill this folder (and the other three) up front, run
`python3 ../download_assets.py` from this folder.
